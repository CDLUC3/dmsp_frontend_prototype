mutation AddRepository($input: AddRepositoryInput) {
  addRepository(input: $input) {
    id
    errors {
      general
      name
      description
      repositoryTypes
      website
    }
    name
    keywords
    uri
    website
  }
}