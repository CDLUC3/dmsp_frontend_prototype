query RecommendedLicenses($recommended: Boolean!) {
  recommendedLicenses(recommended: $recommended) {
    name
    id
    uri
  }
}

query Licenses {
  licenses {
    id
    name
    uri
    recommended
  }
}
