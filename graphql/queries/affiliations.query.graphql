query Affiliations($term: String!, $cursor: Int, $limit: Int) {
  affiliations(term: $term, cursor: $cursor, limit: $limit) {
    cursor
    feed {
      id
      displayName
      uri
    }
    error {
      general
    }
  }
}