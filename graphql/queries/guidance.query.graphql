query GuidanceGroups($affiliationId: String) {
  guidanceGroups(affiliationId: $affiliationId) {
    id
    guidance {
      tags {
        id
        name
        slug
      }
      guidanceText
      id
    }
  }
}
