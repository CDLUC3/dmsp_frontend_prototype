query ProjectContributors($projectId: Int!) {
  projectContributors(projectId: $projectId) {
    id
    givenName
    surName
    orcid
    contributorRoles {
      id
      label
      description
    }
    affiliation {
      displayName
    }
  }
}